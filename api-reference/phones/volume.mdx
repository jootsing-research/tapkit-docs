---
title: Volume
description: Adjust device volume
---

<Note>
Two endpoints: `POST /phones/{phone_id}/volume-up` and `POST /phones/{phone_id}/volume-down`
</Note>

Control the device volume.

## Volume Up

```bash
curl -X POST https://api.tapkit.ai/v1/phones/{phone_id}/volume-up \
  -H "X-API-Key: joot_your_api_key"
```

## Volume Down

```bash
curl -X POST https://api.tapkit.ai/v1/phones/{phone_id}/volume-down \
  -H "X-API-Key: joot_your_api_key"
```

No request body required for either endpoint.
