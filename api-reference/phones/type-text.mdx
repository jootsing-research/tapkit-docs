---
title: Type Text
description: Type text into active field
api: POST /v1/phones/{phone_id}/type
---

Type text into the currently focused input field.

## Request

```bash
curl -X POST https://api.tapkit.ai/v1/phones/{phone_id}/type \
  -H "X-API-Key: joot_your_api_key" \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello, world!", "method": "keys"}'
```

### Request Body

```json
{
  "text": "Hello, world!",
  "method": "keys"
}
```

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `text` | `string` | required | Text to type |
| `method` | `string` | `"keys"` | `"keys"`, `"paste"`, `"shortcut"`, or `"shortcut_legacy"` |

### Methods

| Method | Description |
|--------|-------------|
| `keys` | Simulate individual keystrokes. Works everywhere, slower for long text. |
| `paste` | Paste via clipboard. Fast for long text, may not work in all contexts. |
| `shortcut` | Uses iOS Shortcut for clipboard. Requires [shortcut setup](/setup/shortcuts). Fast and reliable. |
| `shortcut_legacy` | Legacy shortcut-based method. Use `shortcut` instead when possible. |
